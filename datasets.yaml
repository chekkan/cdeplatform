definitions:
  Dataset:
    required:
      - id
      - name
      - description
    properties:
      id:
        type: string
      name:
        type: string
      description:
        type: string
      rating:
        type: integer
